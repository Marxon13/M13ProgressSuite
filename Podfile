source 'https://github.com/CocoaPods/Specs.git'

platform :ios, '9.0'
install! 'cocoapods', :deterministic_uuids => false
use_frameworks!
inhibit_all_warnings!

target 'M13ProgressSuiteDemo' do
  pod 'M13ProgressSuite', :path => '.', :inhibit_warnings => false
end
